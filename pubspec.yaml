name: sandbox
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  flutter_dotenv: ^6.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  pedantic_mono: any


flutter:
  uses-material-design: true
  assets:
    - .env
